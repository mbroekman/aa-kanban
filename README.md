# AA Trello

Alliance Auth plugin skeleton for Trello integration.

## Installation

```bash
pip install aa-trello
```

Add to `local.py`:

```python
INSTALLED_APPS += [
    "aa_trello",
]
```

Run migrations:

```bash
python manage.py migrate
```
